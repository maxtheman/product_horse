# fly.toml app configuration file generated for gpu-product-horse on 2024-08-30T16:07:16-07:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
# change .size to a100-80gb when you are ready

app = 'gpu-product-horse'
primary_region = 'sjc'

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8000
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '4gb'
  cpu_kind = 'shared'
  cpus = 2
